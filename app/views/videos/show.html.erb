<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "← コンテンツに戻る", @content, 
        class: "text-blue-400 hover:text-blue-300 transition-colors" %>
  </div>

  <div class="bg-gray-800 rounded-lg p-8">
    <div class="flex justify-between items-start mb-6">
      <h1 class="text-3xl font-bold text-gray-100">
        <%= @content.theme %> - 動画
      </h1>
      <div class="ml-4 flex-shrink-0">
        <%= render StatusBadge::Component.new(status: @video.status) %>
      </div>
    </div>

    <% if @video.video.present? %>
      <div class="mb-8">
        <video controls class="w-full rounded-lg shadow-lg" style="max-width: 800px;">
          <source src="<%= @video.video.url %>" type="video/mp4">
          お使いのブラウザは動画再生をサポートしていません。
        </video>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        <% if @video.resolution %>
          <div>
            <p class="text-gray-400 text-sm mb-1">解像度</p>
            <p class="text-gray-200 font-medium"><%= @video.resolution %></p>
          </div>
        <% end %>
        
        <% if @video.file_size %>
          <div>
            <p class="text-gray-400 text-sm mb-1">ファイルサイズ</p>
            <p class="text-gray-200 font-medium">
              <% if @video.file_size < 1024 * 1024 %>
                <%= (@video.file_size / 1024.0).round(1) %> KB
              <% else %>
                <%= (@video.file_size / 1024.0 / 1024.0).round(1) %> MB
              <% end %>
            </p>
          </div>
        <% end %>
        
        <% if @video.duration_seconds %>
          <div>
            <p class="text-gray-400 text-sm mb-1">再生時間</p>
            <p class="text-gray-200 font-medium">
              <%= @video.duration_seconds / 60 %>:<%= (@video.duration_seconds % 60).to_s.rjust(2, '0') %>
            </p>
          </div>
        <% end %>
        
        <div>
          <p class="text-gray-400 text-sm mb-1">作成日時</p>
          <p class="text-gray-200"><%= @video.created_at.strftime('%Y/%m/%d %H:%M') %></p>
        </div>
      </div>

      <div class="flex gap-4 mb-8">
        <%= link_to "ダウンロード", @video.video.url, 
              class: "px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors",
              target: "_blank",
              download: "#{@content.theme}_video.mp4" %>
        
        <%= button_to "削除", content_video_path(@content), 
              method: :delete,
              class: "px-6 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition-colors",
              data: {
                turbo_confirm: "動画を削除しますか？"
              } %>
      </div>
    <% else %>
      <div class="text-center py-12">
        <div class="text-6xl mb-4">🎬</div>
        <h2 class="text-xl text-gray-300 mb-2">動画ファイルがありません</h2>
        <p class="text-gray-500">動画の生成が完了していないか、エラーが発生している可能性があります。</p>
        
        <% if @video.error_message %>
          <div class="mt-4 p-4 bg-red-900 bg-opacity-20 border border-red-500 rounded-lg">
            <p class="text-red-400">エラー: <%= @video.error_message %></p>
          </div>
        <% end %>
      </div>
    <% end %>

    <div class="border-t border-gray-700 pt-6">
      <h3 class="text-lg font-semibold text-gray-200 mb-4">技術仕様</h3>
      <div class="bg-gray-900 rounded-lg p-4">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
          <div>
            <span class="text-gray-400">動画コーデック:</span>
            <span class="text-gray-200">H.264 (libx264)</span>
          </div>
          <div>
            <span class="text-gray-400">音声コーデック:</span>
            <span class="text-gray-200">AAC (192kbps, 48kHz)</span>
          </div>
          <div>
            <span class="text-gray-400">フレームレート:</span>
            <span class="text-gray-200">30fps</span>
          </div>
          <div>
            <span class="text-gray-400">最適化:</span>
            <span class="text-gray-200">YouTube推奨設定</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>