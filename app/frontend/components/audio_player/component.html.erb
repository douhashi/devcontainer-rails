<% if should_render_audio_player? %>
  <audio 
    data-controller="audio-player"
    data-audio-player-target="player"
    data-audio-player-autoplay-value="<%= autoplay_value %>"
    data-audio-url="<%= audio_url %>"
    preload="none"
    controls
    class="h-8 max-w-xs"
    style="visibility: hidden; transition: visibility 0.1s;">
    お使いのブラウザはaudio要素をサポートしていません。
  </audio>
<% else %>
  <div class="text-gray-500 text-sm px-2 py-1 rounded">
    処理中...
  </div>
<% end %>