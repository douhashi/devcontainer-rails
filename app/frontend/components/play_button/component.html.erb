<%= render Button::Component.new(
      variant: button_variant,
      size: button_size,
      class: button_custom_class,
      data: button_data,
      id: button_id,
      aria_label: button_aria_label,
      onclick: "this.dispatchEvent(new CustomEvent('track:play', { bubbles: true }))"
    ) do %>
  <%= render Icon::Component.new(name: icon_name, size: :md) %>
<% end %>

